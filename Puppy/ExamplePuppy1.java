public class ExamplePuppy1{ //Does not work if you use (1) simply delete (1) before programming
   
    public ExamplePuppy1(){ //Does not work if you use (1) simply delete (1) before programming
   }

   public ExamplePuppy1(String name){
      // This constructor has one parameter, name.
      
   /* First create an object */
   ObjectReference = new Constructor();

   /* Now call a variable as follows */
   ObjectReference.variableName;

  /* Now you can call a class method as follows */
  ObjectReference.MethodName();    
   }
}
